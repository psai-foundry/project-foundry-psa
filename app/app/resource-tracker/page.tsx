
'use client';

import { ResourceTracker } from '@/components/resource-tracker/ResourceTracker';

export default function ResourceTrackerPage() {
  return (
    <div className="container mx-auto px-4 py-6">
      <ResourceTracker />
    </div>
  );
}
